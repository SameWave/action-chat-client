import now from 'action-chat-client/utils/now';
import { module, test } from 'qunit';

module('Unit | Utility | now');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = now();
  assert.ok(result);
});
