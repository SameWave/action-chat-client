{{tool-bar
  onEdit=(action "goToStreamEditScoreboard")}}

{{#ember-dragula
	config=dragulaconfig
		drag="onDrag"
		dragend="onDragEnd"
		cloned="onCloned"}}
	  {{!-- <h2>Group A</h2> --}}
  {{#sw-ember-dragula}}
    {{#each goals as |item|}}
      {{media-block/goal
        classNames="in-edit js-list-item"
        title=item.title
        date=item.date
        onDeleteGoal=(action "deletGoal")}}
    {{/each}}
  {{/sw-ember-dragula}}
{{/ember-dragula}}